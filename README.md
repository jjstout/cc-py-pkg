# cc-py-pkg

A simple cookie cutter for Python Packages and Tools.

## Host Dependencies
* uv
* git
* git-flow

## Quickstart

```shell
$ cd ~/dev
$ uvx cookiecutter gh:jjstout/cc-py-pkg
$ cd <repo-slug>
$ uv sync
$ source .venv/bin/activate
$ inv init
$ inv push
```
